import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrap: {
    left: 0,
    position: 'absolute',
    right: 0,
  },
  bottom: {
    bottom: 0,
  },
  top: {
    top: 0,
  },
});
